function numberSquare() {
  let input = prompt('Enter a number, get the square!');
  let result = 0;

  result = Number(input) * Number(input);

  alert(result);
}

function bonus() {
  let a = prompt('Enter a number');
  let b = prompt('Enter a second number');
  let result = 0;

  result = (Number(a) * Number(b));

  alert(result);
}
